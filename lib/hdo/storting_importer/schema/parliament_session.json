{
  "id": "hdo#parliament_session",
  "description": "Object representing the parliamentary year.",
  "type":"object",
  "properties": {
    "kind": {
      "type": "string",
      "description": "This is always 'hdo#parliament_session'",
      "default": "hdo#parliament_session",
      "required": true
    },
    "start_date": {
      "type": "string",
      "description": "The date the period started.",
      "format": "date",
      "required": true
    },
    "end_date": {
      "type": "string",
      "description": "The date the period ended.",
      "format": "date",
      "required": true
    }
}
